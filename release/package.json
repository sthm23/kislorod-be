{
  "name": "kislorod-be",
  "version": "0.0.1",
  "description": "Production build - bundled app with external telegraf runtime",
  "scripts": {
    "start": "node dist/main.js"
  },
  "dependencies": {
    "telegraf": "^4.16.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}