{
  "name": "kislorod-be",
  "version": "0.0.1",
  "description": "Production build - all dependencies bundled in dist/main.js",
  "scripts": {
    "start": "node dist/main.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}